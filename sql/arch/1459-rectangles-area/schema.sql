drop schema if exists leetcode;
create schema leetcode;
use leetcode;

Create table If Not Exists Points (id int, x_value int, y_value int);
create table if not exists Expected(p1 int, p2 int, area int);
